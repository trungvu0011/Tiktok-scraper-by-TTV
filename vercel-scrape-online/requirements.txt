# No third-party dependencies — the function uses only the Python standard
# library (urllib, json, re, gzip). This file just signals a Python project to
# Vercel's build step. Keep it empty of packages for the fastest cold start.
